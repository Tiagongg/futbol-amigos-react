rules_version = '2';

// Publicá con: firebase deploy --only storage

service firebase.storage {
  match /b/{bucket}/o {
    match /tournaments/{tournamentId}/players/{fileName} {
      allow read, write: if request.auth != null;
    }
  }
}
